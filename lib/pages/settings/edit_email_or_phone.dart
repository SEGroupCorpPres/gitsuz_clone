import 'package:flutter/material.dart';

class EditEmailOrPhone extends StatelessWidget {
  const EditEmailOrPhone({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
